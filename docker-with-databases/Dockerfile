# language: dockerfile

FROM postgres:15.2

ARG POSTGRES_USER
ENV POSTGRES_USER $POSTGRES_USER

ARG POSTGRES_PASSWORD
ENV POSTGRES_PASSWORD $POSTGRES_PASSWORD

ARG POSTGRES_DB
ENV POSTGRES_DB $POSTGRES_DB

EXPOSE 5432

COPY postgres-database.sql /docker-entrypoint-initdb.d/